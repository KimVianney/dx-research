# Ground truth for planted items. One entry per item.
#
# Schema per entry:
#   id:                stable unique id, e.g. SEC-004
#   wave:              wave number (0..11)
#   repo:              claimline | claimline-edge
#   pr:                PR number (filled when the PR opens; null until then)
#   category:          family/subtype, e.g. security/injection
#   severity_expected: informational | low | medium | high | critical
#   file:              path within the lab repo
#   line:              approx line of the planted item on the PR branch
#   description:       what it is, in plain prose
#   detect_any:        list of substrings; a tool comment on/near the item that
#                      contains ANY of these counts as a detection
#   kind:              defect | decoy | canary
#
# Families: correctness, security (injection/authz/crypto/ssrf/path-traversal/
# deserialization), concurrency, resource-leak, api-contract, performance
# (n-plus-one/quadratic/unbounded-memory), error-handling, test-quality,
# docs-drift, dependency, iac-misconfig, ci-workflow, phi-handling, domain-logic.

probes:
  # ---- W0 PR#2: 12 mixed defects + 2 decoys (feat/claim-batch-import) ----
  - id: W0-CORR-CALC
    wave: 0
    repo: claimline
    pr: 2
    category: correctness/domain-logic
    severity_expected: medium
    file: api/claimline/batch.py
    line: 25
    description: >
      apply_coinsurance applies the coinsurance rate before subtracting the copay
      and can return a negative value; the adjudication engine subtracts the copay
      first and floors at zero. Inconsistent benefit math.
    detect_any: ["copay", "coinsurance", "before", "order", "negative"]
    kind: defect
  - id: W0-SEC-INJ
    wave: 0
    repo: claimline
    pr: 2
    category: security/injection
    severity_expected: critical
    file: api/claimline/imports.py
    line: 25
    description: f-string interpolation of member_id into a SQL WHERE clause.
    detect_any: ["sql inject", "parameteri", "string-based query", "f-string", "bind"]
    kind: defect
  - id: W0-SEC-PATH
    wave: 0
    repo: claimline
    pr: 2
    category: security/path-traversal
    severity_expected: high
    file: api/claimline/imports.py
    line: 39
    description: os.path.join of an unsanitized attachment filename enables path traversal.
    detect_any: ["path travers", "directory travers", "sanitiz", "../", "arbitrary file"]
    kind: defect
  - id: W0-SEC-SSRF
    wave: 0
    repo: claimline
    pr: 2
    category: security/ssrf
    severity_expected: high
    file: api/claimline/imports.py
    line: 32
    description: requests.get on an attacker-controlled attachment_url without validation.
    detect_any: ["ssrf", "server-side request", "validate the url", "unvalidated url", "url validation"]
    kind: defect
  - id: W0-CRYPTO-MD5
    wave: 0
    repo: claimline
    pr: 2
    category: security/crypto
    severity_expected: medium
    file: api/claimline/imports.py
    line: 20
    description: md5 used to derive an idempotency key over member/service data.
    detect_any: ["md5", "insecure hash", "weak hash", "sha-256", "sha256"]
    kind: defect
  - id: W0-RES-LEAK
    wave: 0
    repo: claimline
    pr: 2
    category: resource-leak
    severity_expected: medium
    file: api/claimline/imports.py
    line: 40
    description: file opened for write without closing / context manager.
    detect_any: ["not closed", "context manager", "with open", "resource leak", "file handle", "leak"]
    kind: defect
  - id: W0-ERR-BARE
    wave: 0
    repo: claimline
    pr: 2
    category: error-handling
    severity_expected: medium
    file: api/claimline/imports.py
    line: 61
    description: try/except Exception/pass silently swallows attachment errors.
    detect_any: ["swallow", "silently", "log the exception", "bare except", "except", "pass"]
    kind: defect
  - id: W0-PERF-QUAD
    wave: 0
    repo: claimline
    pr: 2
    category: performance/quadratic
    severity_expected: medium
    file: api/claimline/batch.py
    line: 13
    description: O(n^2) dedup by nested scan over an unbounded claim list.
    detect_any: ["o(n^2)", "o(n2)", "quadratic", "nested loop", "inefficient"]
    kind: defect
  - id: W0-CANARY-SECRET
    wave: 0
    repo: claimline
    pr: 2
    category: security/secret
    severity_expected: critical
    file: api/claimline/storage.py
    line: 8
    description: hardcoded AWS access key id (AKIAIOSFODNN7EXAMPLE, non-functional canary).
    detect_any: ["hardcoded", "secret", "credential", "access key", "aws"]
    kind: canary
  - id: W0-CONC-RACE
    wave: 0
    repo: claimline
    pr: 2
    category: concurrency
    severity_expected: high
    file: worker/batchsettle.go
    line: 18
    description: concurrent writes to a shared map from goroutines without a mutex.
    detect_any: ["race", "concurrent map", "mutex", "lock", "data race", "sync"]
    kind: defect
  - id: W0-SEC-XSS
    wave: 0
    repo: claimline
    pr: 2
    category: security/xss
    severity_expected: high
    file: web/src/ClaimNote.tsx
    line: 9
    description: dangerouslySetInnerHTML renders unsanitized note HTML (XSS).
    detect_any: ["xss", "dangerouslysetinnerhtml", "sanitiz", "cross-site", "untrusted html"]
    kind: defect
  - id: W0-CI-INJECT
    wave: 0
    repo: claimline
    pr: 2
    category: ci-workflow
    severity_expected: high
    file: .github/workflows/pr-attachments.yml
    line: 16
    description: PR title interpolated into a run shell command (expression/script injection).
    detect_any: ["script injection", "expression injection", "injection", "untrusted", "github.event"]
    kind: defect
  - id: W0-SQL-MIGRATE
    wave: 0
    repo: claimline
    pr: 2
    category: database/migration-safety
    severity_expected: high
    file: migrations/0002_add_claim_priority.sql
    line: 5
    description: >
      ADD COLUMN ... NOT NULL without a default on a populated table (fails / full
      table rewrite + lock). Intended defect; was omitted from the first manifest
      draft and added after hand-validation.
    detect_any: ["not null", "default", "lock", "rewrite", "backfill", "table rewrite"]
    kind: defect
  # ---- bonus: real, correct findings CodeRabbit made that were NOT planted ----
  # kind 'bonus' is not scored (recall is measured only against intended defects);
  # recorded for honesty and to credit real value found.
  - id: W0-BONUS-UNITS
    wave: 0
    repo: claimline
    pr: 2
    category: correctness/domain-logic
    severity_expected: medium
    file: api/claimline/batch.py
    line: 38
    description: >
      line_pairs_over_limit compares per-unit charges without multiplying by units,
      inconsistent with Claim.total_charge_cents. A real bug that happened to live in
      the code intended as the bounded-quadratic decoy.
    detect_any: ["units", "charge"]
    kind: bonus
  - id: W0-BONUS-DEP
    wave: 0
    repo: claimline
    pr: 2
    category: dependency/supply-chain
    severity_expected: medium
    file: api/claimline/imports.py
    line: 14
    description: >
      requests imported but not declared in api/pyproject.toml. Real, unplanted;
      CodeRabbit verified it by executing shell/rg scripts (agentic verification).
    detect_any: ["requests", "dependency", "pyproject"]
    kind: bonus
  # ---- decoys (flagging these = false positive) ----
  - id: W0-DECOY-EXCEPT
    wave: 0
    repo: claimline
    pr: 2
    category: error-handling
    severity_expected: informational
    file: api/claimline/imports.py
    line: 78
    description: >
      summarize_import uses a broad except that immediately re-raises after
      finalizing the tally; documented and correct. Flagging as an issue is a FP.
    detect_any: ["broad except", "catch", "swallow", "too general"]
    kind: decoy
  - id: W0-DECOY-BOUNDED-QUAD
    wave: 0
    repo: claimline
    pr: 2
    category: performance/quadratic
    severity_expected: informational
    file: api/claimline/batch.py
    line: 37
    description: >
      INTENDED as a decoy: O(n^2) but hard-bounded (<=8 lines), so flagging the perf
      aspect would be a FP. RESULT: the perf aspect was NOT flagged (good), but the
      code contained an unintended real bug (see W0-BONUS-UNITS), so it is not a clean
      decoy. Marked invalid and excluded from precision scoring.
    detect_any: ["o(n^2)", "quadratic", "nested loop", "performance", "optimize"]
    kind: invalid

  # ==== W1-A correctness family (feat/claim-aggregation-reports, PR #3) ====
  - id: W1C-AGING-GAP
    wave: 1
    repo: claimline
    pr: 3
    category: correctness/boundary
    severity_expected: medium
    file: api/claimline/reporting.py
    line: 17
    description: "aging_bucket: day 31 falls through (> 31 not >= 31), leaving a gap; 31 mis-buckets to 90+."
    detect_any: ["off-by-one", "gap", "boundary", "31", "falls through", "excluded"]
    kind: defect
  - id: W1C-DATE-RANGE
    wave: 1
    repo: claimline
    pr: 3
    category: correctness/boundary
    severity_expected: medium
    file: api/claimline/reporting.py
    line: 29
    description: "daily_totals uses svc < end, excluding the end date though the range is documented [start, end]."
    detect_any: ["inclusive", "exclusive", "end date", "off-by-one", "boundary", "<= end"]
    kind: defect
  - id: W1C-AVG-TRUNC
    wave: 1
    repo: claimline
    pr: 3
    category: correctness
    severity_expected: low
    file: api/claimline/reporting.py
    line: 37
    description: "average_paid uses integer floor division (loses cents) and has no guard for an empty list (ZeroDivisionError)."
    detect_any: ["integer division", "truncat", "floor", "zerodivision", "divide by zero", "empty"]
    kind: defect
  - id: W1C-PCT-TRUNC
    wave: 1
    repo: claimline
    pr: 3
    category: correctness
    severity_expected: low
    file: api/claimline/reporting.py
    line: 42
    description: "member_responsibility_pct truncates via int() and divides by allowed_cents which may be 0."
    detect_any: ["truncat", "integer", "zerodivision", "divide by zero", "round"]
    kind: defect
  - id: W1C-LATEFEE-SIGN
    wave: 1
    repo: claimline
    pr: 3
    category: correctness
    severity_expected: high
    file: api/claimline/reporting.py
    line: 47
    description: "apply_late_fee subtracts the fee from the balance; it should add it."
    detect_any: ["sign", "subtract", "should add", "increase", "minus", "wrong operator"]
    kind: defect
  - id: W1C-TIMELY-BOUND
    wave: 1
    repo: claimline
    pr: 3
    category: correctness/boundary
    severity_expected: medium
    file: api/claimline/reporting.py
    line: 52
    description: "is_timely uses < limit_days, so a claim filed exactly on the limit day is wrongly untimely (should be <=)."
    detect_any: ["off-by-one", "boundary", "<=", "inclusive", "exactly", "equal"]
    kind: defect
  - id: W1C-MUT-DEFAULT
    wave: 1
    repo: claimline
    pr: 3
    category: correctness/foot-gun
    severity_expected: medium
    file: api/claimline/reporting.py
    line: 55
    description: "merge_adjustments has a mutable dict default and mutates base in place."
    detect_any: ["mutable default", "default argument", "b006", "shared", "mutates"]
    kind: defect
  - id: W1C-TOP-ORDER
    wave: 1
    repo: claimline
    pr: 3
    category: correctness
    severity_expected: high
    file: api/claimline/reporting.py
    line: 64
    description: "top_payers sorts ascending (no reverse), returning the lowest n rather than the highest."
    detect_any: ["reverse", "descending", "ascending", "highest", "lowest", "sort order"]
    kind: defect
  - id: W1C-ROUND-TRUNC
    wave: 1
    repo: claimline
    pr: 3
    category: correctness
    severity_expected: medium
    file: api/claimline/reporting.py
    line: 70
    description: "round_to_dollar truncates via int() instead of rounding to nearest dollar."
    detect_any: ["truncat", "round", "nearest", "floor"]
    kind: defect
  - id: W1C-BALANCE-ACC
    wave: 1
    repo: claimline
    pr: 3
    category: correctness
    severity_expected: high
    file: api/claimline/reporting.py
    line: 78
    description: "running_balance overwrites balance each iteration instead of accumulating (should be +=)."
    detect_any: ["accumulat", "running", "+=", "overwrit", "reset", "each iteration"]
    kind: defect
  # ---- decoys (correct despite looking suspicious) ----
  - id: W1C-DECOY-HALFUP
    wave: 1
    repo: claimline
    pr: 3
    category: correctness
    severity_expected: informational
    file: api/claimline/reporting.py
    line: 85
    description: "half_up_round uses Decimal ROUND_HALF_UP correctly; flagging float-precision here is a FP."
    detect_any: ["float", "precision", "rounding error", "inaccurate"]
    kind: decoy
  - id: W1C-DECOY-INCLUSIVE
    wave: 1
    repo: claimline
    pr: 3
    category: correctness/boundary
    severity_expected: informational
    file: api/claimline/reporting.py
    line: 92
    description: "inclusive_day_count's +1 is intentional and documented; flagging off-by-one is a FP."
    detect_any: ["off-by-one", "incorrect", "should not add", "boundary error"]
    kind: decoy
  - id: W1C-DECOY-SAFERATIO
    wave: 1
    repo: claimline
    pr: 3
    category: correctness
    severity_expected: informational
    file: api/claimline/reporting.py
    line: 98
    description: "safe_ratio guards a zero denominator; flagging divide-by-zero is a FP."
    detect_any: ["divide by zero", "zerodivision", "division by zero"]
    kind: decoy


  # ==== W1-B security family, HELPER-FUNCTION form (feat/claim-document-helpers, PR #8) ====
  # Committable form (plain helpers, no live endpoints); reachability tested separately in W2.
  - id: W1SH-SQLI
    wave: 1
    repo: claimline
    pr: 8
    category: security/injection
    severity_expected: critical
    file: api/claimline/documents.py
    line: 24
    description: "find_documents_sql f-string-interpolates member_id into SQL."
    detect_any: ["sql inject", "parameteri", "bind", "string-based query", "f-string"]
    kind: defect
  - id: W1SH-SSRF
    wave: 1
    repo: claimline
    pr: 8
    category: security/ssrf
    severity_expected: high
    file: api/claimline/documents.py
    line: 35
    description: "fetch_external_document urlopens a caller-supplied url with no validation (SSRF)."
    detect_any: ["ssrf", "server-side request", "validate the url", "url validation", "unvalidated"]
    kind: defect
  - id: W1SH-TIMEOUT
    wave: 1
    repo: claimline
    pr: 8
    category: resource-leak
    severity_expected: medium
    file: api/claimline/documents.py
    line: 40
    description: "poll_conversion_status urlopen has no timeout (can hang / DoS)."
    detect_any: ["timeout", "hang", "block indefinitely", "denial of service"]
    kind: defect
  - id: W1SH-PATH
    wave: 1
    repo: claimline
    pr: 8
    category: security/path-traversal
    severity_expected: high
    file: api/claimline/documents.py
    line: 45
    description: "save_document os.path.join(DOCS_ROOT, name) with unsanitized name (traversal)."
    detect_any: ["path travers", "directory travers", "sanitiz", "../", "arbitrary file"]
    kind: defect
  - id: W1SH-MD5
    wave: 1
    repo: claimline
    pr: 8
    category: security/crypto
    severity_expected: medium
    file: api/claimline/documents.py
    line: 53
    description: "document_token derived from md5 (weak, also predictable share token)."
    detect_any: ["md5", "insecure hash", "weak hash", "sha-256", "sha256"]
    kind: defect
  - id: W1SH-RANDOM
    wave: 1
    repo: claimline
    pr: 8
    category: security/crypto
    severity_expected: high
    file: api/claimline/documents.py
    line: 63
    description: "new_share_id uses random.choice (non-cryptographic) for an unguessable id."
    detect_any: ["random", "cryptograph", "secrets module", "predictable", "insecure random"]
    kind: defect
  - id: W1SH-IDOR
    wave: 1
    repo: claimline
    pr: 8
    category: security/authz
    severity_expected: high
    file: api/claimline/documents.py
    line: 66
    description: "get_claim_for_user ignores user_id (no ownership check; IDOR/BOLA)."
    detect_any: ["authorization", "authz", "idor", "access control", "ownership", "unused", "bola"]
    kind: defect
  - id: W1SH-XSS
    wave: 1
    repo: claimline
    pr: 8
    category: security/xss
    severity_expected: high
    file: api/claimline/documents.py
    line: 73
    description: "render_note interpolates user text into HTML without escaping (XSS)."
    detect_any: ["xss", "cross-site", "escap", "sanitiz", "html injection"]
    kind: defect
  - id: W1SH-LOG
    wave: 1
    repo: claimline
    pr: 8
    category: phi-handling
    severity_expected: medium
    file: api/claimline/documents.py
    line: 83
    description: "log_access logs a full member record and a token at info (sensitive data / PHI in logs)."
    detect_any: ["log", "sensitive", "pii", "phi", "redact", "token in log"]
    kind: defect
  # decoys
  - id: W1SH-DECOY-PARAM
    wave: 1
    repo: claimline
    pr: 8
    category: security/injection
    severity_expected: informational
    file: api/claimline/documents.py
    line: 29
    description: "find_documents_safe uses a bound ? parameter; flagging SQLi is a FP."
    detect_any: ["sql inject", "injection", "not parameterized", "unsafe query"]
    kind: decoy
  - id: W1SH-DECOY-SHA256
    wave: 1
    repo: claimline
    pr: 8
    category: security/crypto
    severity_expected: informational
    file: api/claimline/documents.py
    line: 58
    description: "content_digest uses sha256 for a non-secret integrity digest; flagging weak-crypto is a FP."
    detect_any: ["weak hash", "insecure hash", "md5", "broken crypto"]
    kind: decoy
  - id: W1SH-DECOY-ESCAPE
    wave: 1
    repo: claimline
    pr: 8
    category: security/xss
    severity_expected: informational
    file: api/claimline/documents.py
    line: 78
    description: "render_note_safe html.escapes the text; flagging XSS is a FP."
    detect_any: ["xss", "cross-site", "not escaped", "html injection"]
    kind: decoy

  # ==== W1-C concurrency family (feat/worker-concurrent-aggregation, PR #9) ====
  # Go worker helpers; go vet is clean, go test -race would catch the data races.
  - id: W1CC-ADD-IN-GO
    wave: 1
    repo: claimline
    pr: 9
    category: concurrency
    severity_expected: high
    file: worker/concurrent.go
    line: 15
    description: "wg.Add(1) called inside the goroutine; Wait can return before goroutines register."
    detect_any: ["waitgroup", "wg.add", "add before", "race", "before starting", "outside the goroutine"]
    kind: defect
  - id: W1CC-MAP-RACE
    wave: 1
    repo: claimline
    pr: 9
    category: concurrency
    severity_expected: high
    file: worker/concurrent.go
    line: 18
    description: "concurrent writes to the shared totals map without a mutex (data race / concurrent map write)."
    detect_any: ["race", "concurrent map", "mutex", "lock", "data race", "synchroniz"]
    kind: defect
  - id: W1CC-COUNTER-RACE
    wave: 1
    repo: claimline
    pr: 9
    category: concurrency
    severity_expected: high
    file: worker/concurrent.go
    line: 45
    description: "Counter.Bump increments a shared int64 from goroutines without atomic/mutex (data race)."
    detect_any: ["race", "atomic", "mutex", "data race", "synchroniz", "counter"]
    kind: defect
  - id: W1CC-GOLEAK
    wave: 1
    repo: claimline
    pr: 9
    category: concurrency
    severity_expected: medium
    file: worker/concurrent.go
    line: 58
    description: "NotifyFirst leaks a goroutine: unbuffered send blocks forever after the single receive."
    detect_any: ["goroutine leak", "leak", "blocks forever", "unbuffered", "deadlock", "never receiv"]
    kind: defect
  - id: W1CC-APPEND-RACE
    wave: 1
    repo: claimline
    pr: 9
    category: concurrency
    severity_expected: high
    file: worker/concurrent.go
    line: 73
    description: "concurrent append to a shared slice from goroutines (data race)."
    detect_any: ["race", "append", "shared slice", "data race", "mutex", "synchroniz"]
    kind: defect
  - id: W1CC-UNLOCK
    wave: 1
    repo: claimline
    pr: 9
    category: concurrency
    severity_expected: high
    file: worker/concurrent.go
    line: 91
    description: "SafeStore.Add returns early with the mutex still locked (no defer Unlock) -> deadlock."
    detect_any: ["unlock", "defer", "lock held", "deadlock", "early return"]
    kind: defect
  - id: W1CC-TICK-LEAK
    wave: 1
    repo: claimline
    pr: 9
    category: resource-leak
    severity_expected: medium
    file: worker/concurrent.go
    line: 103
    description: "time.Tick inside a select loop creates a new leaking ticker each iteration."
    detect_any: ["time.tick", "ticker", "leak", "time.newticker", "stop()"]
    kind: defect
  # decoys (correct concurrency)
  - id: W1CC-DECOY-MUTEX
    wave: 1
    repo: claimline
    pr: 9
    category: concurrency
    severity_expected: informational
    file: worker/concurrent.go
    line: 126
    description: "SafeAggregate is correctly mutex-guarded; flagging a race here is a FP."
    detect_any: ["race", "not synchronized", "unguarded", "data race"]
    kind: decoy
  - id: W1CC-DECOY-ONCE
    wave: 1
    repo: claimline
    pr: 9
    category: concurrency
    severity_expected: informational
    file: worker/concurrent.go
    line: 142
    description: "Config uses sync.Once correctly; flagging a race here is a FP."
    detect_any: ["race", "not synchronized", "double init", "data race"]
    kind: decoy
  - id: W1CC-DECOY-BUFCHAN
    wave: 1
    repo: claimline
    pr: 9
    category: concurrency
    severity_expected: informational
    file: worker/concurrent.go
    line: 150
    description: "BufferedFanIn sizes the channel to the sender count (no leak); flagging a leak is a FP."
    detect_any: ["goroutine leak", "leak", "unbuffered", "blocks"]
    kind: decoy

  # ==== W1-D performance family (feat/claim-analytics, PR #10) ====
  # ruff-clean: no deterministic catches (PERF rules not enabled) -> LLM-only territory.
  - id: W1P-NPLUS1
    wave: 1
    repo: claimline
    pr: 10
    category: performance/n-plus-one
    severity_expected: high
    file: api/claimline/analytics.py
    line: 15
    description: "totals_by_member issues one fetch_claims call per member (N+1)."
    detect_any: ["n+1", "n plus one", "per member", "batch", "single query", "one query per"]
    kind: defect
  - id: W1P-UNBOUNDED
    wave: 1
    repo: claimline
    pr: 10
    category: performance/unbounded-memory
    severity_expected: medium
    file: api/claimline/analytics.py
    line: 22
    description: "load_all_lines reads the whole file into memory (unbounded); also no context manager."
    detect_any: ["unbounded", "whole file", "into memory", "stream", "memory", "context manager"]
    kind: defect
  - id: W1P-RECOMPUTE
    wave: 1
    repo: claimline
    pr: 10
    category: performance/quadratic
    severity_expected: high
    file: api/claimline/analytics.py
    line: 29
    description: "running_share recomputes the grand total inside the loop (O(n^2))."
    detect_any: ["o(n^2)", "quadratic", "recompute", "hoist", "outside the loop", "each iteration"]
    kind: defect
  - id: W1P-STRCONCAT
    wave: 1
    repo: claimline
    pr: 10
    category: performance
    severity_expected: low
    file: api/claimline/analytics.py
    line: 38
    description: "build_csv concatenates strings with += in a loop (quadratic string building)."
    detect_any: ["string concat", "join", "+=", "quadratic", "stringbuilder", "io.stringio"]
    kind: defect
  - id: W1P-SORTLOOP
    wave: 1
    repo: claimline
    pr: 10
    category: performance/quadratic
    severity_expected: high
    file: api/claimline/analytics.py
    line: 46
    description: "rank_within_groups re-sorts (and re-flattens) all amounts inside the per-group loop."
    detect_any: ["sort", "inside the loop", "recompute", "hoist", "outside the loop", "o(n"]
    kind: defect
  - id: W1P-LISTMEMBER
    wave: 1
    repo: claimline
    pr: 10
    category: performance
    severity_expected: medium
    file: api/claimline/analytics.py
    line: 53
    description: "filter_active does membership tests against a list (O(n*m); should use a set)."
    detect_any: ["set", "membership", "o(n", "list lookup", "hash", "dictionary"]
    kind: defect
  - id: W1P-REGEXLOOP
    wave: 1
    repo: claimline
    pr: 10
    category: performance
    severity_expected: low
    file: api/claimline/analytics.py
    line: 60
    description: "match_procedure_codes compiles the regex inside the loop each iteration."
    detect_any: ["compile", "precompile", "outside the loop", "re.compile", "hoist", "module level"]
    kind: defect
  - id: W1P-LOADFILTER
    wave: 1
    repo: claimline
    pr: 10
    category: performance
    severity_expected: medium
    file: api/claimline/analytics.py
    line: 68
    description: "claims_with_status selects all rows then filters status in Python (should be a WHERE)."
    detect_any: ["where", "filter in the database", "in python", "sql", "predicate", "push down"]
    kind: defect
  # decoys (efficient; flagging as perf issues = FP)
  - id: W1P-DECOY-STREAM
    wave: 1
    repo: claimline
    pr: 10
    category: performance/unbounded-memory
    severity_expected: informational
    file: api/claimline/analytics.py
    line: 77
    description: "stream_lines is a streaming generator; flagging unbounded memory is a FP."
    detect_any: ["unbounded", "into memory", "load all", "memory"]
    kind: decoy
  - id: W1P-DECOY-SET
    wave: 1
    repo: claimline
    pr: 10
    category: performance
    severity_expected: informational
    file: api/claimline/analytics.py
    line: 86
    description: "filter_active_fast already uses a set; flagging O(n) membership is a FP."
    detect_any: ["set", "o(n", "membership", "list lookup"]
    kind: decoy
  - id: W1P-DECOY-HOIST
    wave: 1
    repo: claimline
    pr: 10
    category: performance/quadratic
    severity_expected: informational
    file: api/claimline/analytics.py
    line: 91
    description: "shares_precomputed hoists the grand total; flagging recompute is a FP."
    detect_any: ["recompute", "inside the loop", "each iteration", "quadratic"]
    kind: decoy

  # ==== W1-E error-handling family (feat/claim-error-handling, PR #11) ====
  - id: W1E-SWALLOW
    wave: 1
    repo: claimline
    pr: 11
    category: error-handling
    severity_expected: high
    file: api/claimline/remittance.py
    line: 18
    description: "post_remittance swallows all exceptions (except: pass) and returns True regardless."
    detect_any: ["swallow", "silently", "except", "pass", "returns true", "log"]
    kind: defect
  - id: W1E-RETURN-NONE
    wave: 1
    repo: claimline
    pr: 11
    category: error-handling
    severity_expected: medium
    file: api/claimline/remittance.py
    line: 28
    description: "load_payer_config catches Exception and returns None, hiding failures from callers."
    detect_any: ["broad except", "return none", "hide", "swallow", "specific exception"]
    kind: defect
  - id: W1E-NO-FROM
    wave: 1
    repo: claimline
    pr: 11
    category: error-handling
    severity_expected: low
    file: api/claimline/remittance.py
    line: 36
    description: "parse_amount re-raises RemittanceError without `from`, losing the original cause."
    detect_any: ["from", "raise from", "chain", "cause", "b904", "context"]
    kind: defect
  - id: W1E-BROAD-DEBUG
    wave: 1
    repo: claimline
    pr: 11
    category: error-handling
    severity_expected: high
    file: api/claimline/remittance.py
    line: 47
    description: "settle wraps a large block in except Exception, logs at debug, and returns bad state."
    detect_any: ["broad", "too broad", "debug", "bad state", "swallow", "specific"]
    kind: defect
  - id: W1E-NO-FINALLY
    wave: 1
    repo: claimline
    pr: 11
    category: resource-leak
    severity_expected: medium
    file: api/claimline/remittance.py
    line: 53
    description: "write_payer_file opens a file without with/finally; a write error leaks the handle."
    detect_any: ["with", "context manager", "finally", "not closed", "leak", "close"]
    kind: defect
  - id: W1E-BARE-EXC
    wave: 1
    repo: claimline
    pr: 11
    category: error-handling
    severity_expected: low
    file: api/claimline/remittance.py
    line: 63
    description: "require_payer raises a generic Exception instead of a specific type."
    detect_any: ["specific exception", "generic", "bare exception", "custom exception", "too general"]
    kind: defect
  - id: W1E-IGNORE-RET
    wave: 1
    repo: claimline
    pr: 11
    category: error-handling
    severity_expected: medium
    file: api/claimline/remittance.py
    line: 69
    description: "reverse_if_denied calls gateway.reverse without checking success or handling failure of a financial reversal."
    detect_any: ["return value", "ignored", "check the result", "error handling", "unchecked", "success"]
    kind: defect
  # decoys (correct error handling)
  - id: W1E-DECOY-FROM
    wave: 1
    repo: claimline
    pr: 11
    category: error-handling
    severity_expected: informational
    file: api/claimline/remittance.py
    line: 81
    description: "parse_amount_ok correctly chains with `from exc`; flagging is a FP."
    detect_any: ["raise from", "missing from", "chain", "cause"]
    kind: decoy
  - id: W1E-DECOY-WITH
    wave: 1
    repo: claimline
    pr: 11
    category: resource-leak
    severity_expected: informational
    file: api/claimline/remittance.py
    line: 86
    description: "write_payer_file_ok uses a with-block; flagging a leak is a FP."
    detect_any: ["not closed", "context manager", "leak", "finally"]
    kind: decoy
  - id: W1E-DECOY-RERAISE
    wave: 1
    repo: claimline
    pr: 11
    category: error-handling
    severity_expected: informational
    file: api/claimline/remittance.py
    line: 96
    description: "post_remittance_ok logs and re-raises a narrow ConnectionError; flagging is a FP."
    detect_any: ["swallow", "broad except", "should re-raise", "silently"]
    kind: decoy

  # ==== PHI/domain family (feat/claim-phi, PR #30) ====
  # Healthcare-RCM PHI handling. Probes whether CodeRabbit (and/or a `presidio`
  # integration, which has not appeared in 110+ prior comments) flags PHI/PII
  # leakage. Synthetic member data only; helper functions, no live endpoints.
  - id: PHI-LOG
    wave: phi
    repo: claimline
    pr: 30
    category: data-privacy
    severity_expected: high
    file: api/claimline/phi.py
    line: 28
    description: "log_eligibility_check logs member name, DOB, and SSN at INFO (PHI in logs)."
    detect_any: ["phi", "pii", "sensitive", "ssn", "log", "redact", "mask", "presidio", "hipaa", "personal"]
    kind: defect
  - id: PHI-ERROR-MSG
    wave: phi
    repo: claimline
    pr: 30
    category: data-privacy
    severity_expected: medium
    file: api/claimline/phi.py
    line: 40
    description: "coverage_error embeds name/DOB/SSN in an error string that may surface to clients/logs."
    detect_any: ["phi", "pii", "sensitive", "ssn", "error message", "leak", "expose", "redact", "presidio"]
    kind: defect
  - id: PHI-URL-QUERY
    wave: phi
    repo: claimline
    pr: 30
    category: data-privacy
    severity_expected: high
    file: api/claimline/phi.py
    line: 49
    description: "eligibility_url places SSN and DOB in URL query params (logged by proxies/servers)."
    detect_any: ["phi", "pii", "ssn", "query string", "url", "query param", "logged", "sensitive", "header", "body", "presidio"]
    kind: defect
  - id: PHI-API-BOUNDARY
    wave: phi
    repo: claimline
    pr: 30
    category: data-privacy
    severity_expected: medium
    file: api/claimline/phi.py
    line: 60
    description: "payer_submission_payload sends the full member record (incl SSN/DOB) when only member_id is needed (minimum necessary)."
    detect_any: ["phi", "pii", "ssn", "minimum necessary", "over-share", "full record", "only member_id", "data minimization", "sensitive", "presidio"]
    kind: defect
  - id: PHI-FIXTURE
    wave: phi
    repo: claimline
    pr: 30
    category: data-privacy
    severity_expected: low
    file: api/claimline/phi.py
    line: 65
    description: "SAMPLE_MEMBER hardcodes realistic-looking unredacted PHI (name/DOB/SSN) in source."
    detect_any: ["phi", "pii", "ssn", "hardcoded", "sample", "fixture", "test data", "synthetic", "fake", "redact", "presidio"]
    kind: defect
  - id: PHI-DECOY-REDACTED
    wave: phi
    repo: claimline
    pr: 30
    category: data-privacy
    severity_expected: informational
    file: api/claimline/phi.py
    line: 86
    description: "log_eligibility_check_safe already masks the SSN via _mask_ssn; flagging it for PHI is a FP."
    detect_any: ["phi", "pii", "ssn", "leak", "unmasked", "redact"]
    kind: decoy
